# SimpleMindMap for fnOS

SimpleMindMap is a FlyOS (fnOS) web application that provides server-side, user-isolated storage for mind-map documents.

## Upstream source

This project is a Fork-based adaptation of [wanglin2/mind-map](https://github.com/wanglin2/mind-map). The original browser mind-map editor and its related source remain credited to the upstream project.

## Packaging

Release artifacts are published as `.fpk` packages for fnOS. Build the web application with Node.js, then use `fnpack build -d .` from the repository root.
